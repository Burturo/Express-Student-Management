<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Connexion</title>
  <link rel="stylesheet" href="/stylesheets/login.css">
  <link rel="stylesheet" href="/lib/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/lib/bootstrap/css/bootstrap.min.css">
  <style>
    
  </style>
  <!-- Styles internes pour les liens d'inscription. -->
</head>
<body>
  <div class="container">
    <!-- Début du conteneur principal. -->
    <form action="/registration" method="post" class="conteneur card d-flex flex-row mx-auto">
      <!-- Début du formulaire d'inscription. -->
      <div class="col-12 col-lg-6 p-4 d-flex flex-column">
        <div class="text-center">
          <h2>Inscription</h2>
        </div>
        <div class="mb-2 mt-4">
          <input type="text" class="form-control" id="nom" aria-describedby="emailHelp" name="nom" placeholder="Nom d'utilisateur">
          <!-- Champ pour le nom d'utilisateur. -->
        </div>
        <div class="mb-2 mt-4">
          <input type="mail" class="form-control" id="mail" aria-describedby="emailHelp" name="mail" placeholder="Adresse E-mail">
          <!-- Champ pour l'adresse email. -->
        </div>
        <div class="mb-2 mt-4">
          <input type="password" class="form-control" id="password" aria-describedby="emailHelp" name="password" placeholder="Mot de Passe">
          <!-- Champ pour le mot de passe. -->
        </div>
        <div class="mb-2 mt-4">
          <input type="password" class="form-control" id="confirme_pass" aria-describedby="emailHelp" name="confirme_pass" placeholder="Confirmez le mot de passe">
          <!-- Champ pour confirmer le mot de passe. -->
        </div>
        <div class="mb-2 mt-4">
          <select class="form-select" aria-label="Default select example" name="direction">
            <option value="" selected>Sélectionner la direction</option>
            <option value="Etudiant">Etudiant</option>
            <option value="Professeur">Professeur</option>
          </select>
        </div>
        <input type="submit" name="submit" class="btn btn-bleu my-4" value="S'inscrire">
        <!-- Bouton pour soumettre le formulaire. -->
        <div class="form-btn text-center">
          <p>Vous avez déjà un compte ? <a class="register-link" href="/">Retour</a></p>
          <!-- Lien vers la page de connexion si l'utilisateur a déjà un compte. -->
        </div>
      </div>
      <div class="col d-none d-md-inline-flex">
        <div class="image-connexion">
          <div class="image">
            <img width="100%" height="100%" class="img-logo" src="/images/programme.jpg" alt="">
            <!-- Image d'illustration pour la connexion. -->
          </div>
        </div>
      </div>
    </form>
  </div>
</body>
</html>
